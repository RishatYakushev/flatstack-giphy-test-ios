# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'flatstack-giphy-test-ios' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  pod 'Kingfisher'
  pod 'GiphyCoreSDK'

  # Pods for flatstack-giphy-test-ios

  target 'flatstack-giphy-test-iosTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'flatstack-giphy-test-iosUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
